syntax = "proto3";

option java_multiple_files = true;
package com.andi.grpcserver.pingpong;

message PingPongMessage {
	string pingpong = 1;
}

service PingPongService {
	rpc play (PingPongMessage) returns (PingPongMessage);
}